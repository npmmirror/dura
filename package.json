{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1",
    "ts-jest": "^26.3.0",
    "typescript": "^4.0.2"
  },
  "scripts": {
    "build": "npx lerna run build",
    "build:w": "npx lerna run build:w",
    "test": "npx jest",
    "test:w": "npx jest --watchAll"
  }
}
